<% recipes.each do |recipe| %>
  <%= link_to recipe_path(recipe) do %>
    <%= attachment_image_tag recipe, :recipe_image, :fill, 300, 300, fallback: "no_image.jpg", size: "300x300" %>
    <%= recipe.recipe_name %>
  <% end %>
<% end %>